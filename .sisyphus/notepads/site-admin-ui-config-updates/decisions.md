- Task 4 decision: ImageKit upload auth is implemented with a tiny server-only helper using Node `crypto` instead of adding an SDK, returns only the signed upload payload plus a fixed `/pages/home` folder, and exposes a small `toGeneralConfigAsset()` mapper so uploaded asset metadata can be normalized into the existing `GeneralConfigAsset` contract without changing persistence rules.

- Task 6 Journey contract: keep `src/app/[locale]/journey/page.tsx` as the server fetch/format boundary and define Journey interaction rules in `journey-client.tsx`, with default mode fixed to `'map'`.
- Task 6 Journey search contract: reuse a single `normalizeJourneySearchText()` helper for lowercase + diacritic removal so future list/map matching stays case-insensitive and accent-insensitive from one client-side source of truth.
- Task 6 Journey selector contract: expose stable test hooks for `journey-search-input`, `journey-mode-toggle`, `journey-list-results`, `journey-empty-state`, and per-province `journey-map-region-{id}` selectors so Task 7 can build UI behavior without inventing identifiers.
- Task 6 Journey map contract: while a search is active, only matching provinces with trips remain interactive; muted non-match provinces render as non-interactive and cannot open the province detail sheet.
- Task 7 Journey UX decision: keep the detailed province sheet bound to the full trip dataset and implement filtering only in the visible list/map layer, so search never changes the media experience after a valid province is opened and muted provinces simply stay non-clickable during active search.
