- Task 1 verified: header nav pills render without the old outer wrapper, language switch keeps accessible labeling without the icon, and footer contains a hidden hotspot author link.
- Task 2 verified: GeneralConfig types/model resolve deterministic defaults and merge partial persisted data safely.
- Task 3 fix: `src/app/api/general-config/route.ts` now validates the full allowed v1 write payload with strict `zod` objects before singleton upsert, so malformed or extra fields are rejected with `400` while `GET` still uses `resolveGeneralConfig` for deterministic fallback reads.
- Task 3 follow-up: the same route now catches malformed `request.json()` failures and returns `400` JSON instead of surfacing a server error, and validation errors are formatted from `error.issues` to avoid deprecated Zod helpers.
- Task 4 pattern: protected admin-only API routes continue to use Better Auth server sessions via `auth.api.getSession({ headers: await headers() })` plus `ADMIN_USER_EMAIL.includes(session.user.email)` for the allowlist gate.
- Task 4 follow-up: `src/lib/imagekit-server.ts` now treats `IMAGEKIT_URL_ENDPOINT` as part of the required server-side ImageKit config contract together with the public/private keys, while keeping the upload-auth response narrow and preserving the fixed `/pages/home` folder plus existing `GeneralConfigAsset` mapping.
- Task 7 Journey UI pattern: keep search/filtering fully client-side in `journey-client.tsx` by deriving `visibleTrips`, `visibleProvinceIds`, and map highlight/interactivity from the existing `Trip` objects plus `normalizeJourneySearchText()`, so map and list views stay in sync from one source of truth.
- Task 7 follow-up: Journey search matching must include participant names in the same normalized client-side searchable text as trip title, summary, and province name, or valid people-based queries will incorrectly show empty results and muted maps.
- Task 7 final-wave fix: when search is active, map highlight and clickability must come directly from province IDs present in filtered `visibleTrips`; a separate province-name-only widening step causes clickable provinces whose detail sheet has no matching trips.
- Task 7 final consistency fix: `ProvinceDetailSheet` should receive `visibleTrips` rather than the full trip array so an active search cannot reopen filtered-out trips inside an otherwise matching province.
- Task 5 pattern: keep homepage public images bound to resolved `GeneralConfig` on the server, but keep inline admin upload drafts isolated in a Home-only client panel so ImageKit uploads only update local preview until a full `/api/general-config` save succeeds; merge saves with the last resolved config object to preserve untouched non-home fields.
