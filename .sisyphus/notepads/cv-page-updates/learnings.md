# Learnings

- `/[locale]/cv` is routed through `src/app/[locale]/cv/page.tsx` and rendered primarily by `src/components/cv/cv-page-shell.tsx`.
- Shared header/footer are hidden on `/cv`, so login/edit controls must live inside the CV shell itself.
- Current export behavior uses `react-to-print`; scope is to tighten print/save-as-PDF, not add true PDF generation.
- Experience styling for both layouts is shared through `ExperienceSection` in `src/components/cv/cv-layouts.tsx`.
- Task 1 updated the `/[locale]/cv` route payload to pass `isAdmin` from Better Auth session + `ADMIN_USER_EMAIL`, while renaming CV message keys to `fullLayout`, `minimalLayout`, `hrAssistant`, and `hrAssistantDescription`.
- Verified Task 1 with clean LSP diagnostics on the changed TSX files, a successful `npm run build`, and a stale-label scan showing the old active English CV labels were removed from `messages/en.json` and `messages/vi.json`.
- Task 2 refactored `src/components/cv/cv-page-shell.tsx` into one fixed top-left action cluster, defaulting the initial layout client-side to minimal below `md` and full at `md+`, while preserving manual toggling after mount.
- `src/components/switch-language.tsx` and `src/components/cv-download-button.tsx` now support the CV shell’s icon-only mobile controls with explicit `aria-label`/`title` coverage and labeled controls from `sm` upward.
- Added client-side `useSession` visibility in the CV shell so public visitors see Google login, admins still get the server-derived edit link, and `npm run build` passed after the change.
- Task 5 keeps `src/components/interview-chat-widget.tsx` structurally unchanged and preserves `/api/interview-chat`, while rewriting all recruiter-facing copy, seeded prompts, form labels, and scheduling summaries in both English and Vietnamese for HR outreach to the candidate.
- Follow-up Task 2 fix replaced the `useEffect`-driven `setLayoutMode(...)` call in `src/components/cv/cv-page-shell.tsx` with a lazy `useState` initializer using `window.matchMedia`, preserving the post-hydration mobile/desktop default and manual toggle behavior without triggering React's `set-state-in-effect` lint rule.
- Verification for the retry required one unrelated baseline lint cleanup in `src/app/[locale]/journey/add/_components/add-trip-form.tsx` (removing an unused destructured `value`), after which both `npm run lint` and `npm run build` passed.
- Task 4 normalized the CV action cluster sizing by moving the shared pill dimensions into `src/components/cv/cv-page-shell.tsx` and letting `src/components/switch-language.tsx` plus `src/components/cv-download-button.tsx` inherit matching `min-h-11/min-w-11` mobile hit areas with aligned `sm` padding, while preserving icon-only mobile labels and the existing locale/layout/print handlers.
- Task 6 keeps both full and minimal CV layouts on the shared `ExperienceSection` in `src/components/cv/cv-layouts.tsx`, moving the vertical timeline rule to the section-level list container so the spine stays continuous while removing the per-item bordered card wrapper.
- Task 3 tightened `src/components/cv/cv-page-shell.tsx` print/export by attaching `react-to-print` directly to the hidden `CVDesktopLayout` wrapper and removing the extra padded inner print div, so browser print/save-as-PDF targets only the desktop CV document without app controls or added wrapper whitespace.
- Task 4 retry fixed the last mobile control mismatch by making `src/components/switch-language.tsx` use one square visible icon footprint below `sm` (language glyph with an overlaid flag cue) and adding a transparent border to the primary mobile button class in `src/components/cv/cv-page-shell.tsx`, which brought the language/layout/download/login controls onto the same outer mobile size without changing desktop labels or action behavior.
- Final-wave blocker fix updated `src/app/api/interview-chat/route.ts` so the system prompt and fallback replies stay consistently recruiter/HR-oriented in English and Vietnamese, and adjusted `src/components/cv/cv-page-shell.tsx` to render a responsive SSR fallback for the default full layout so mobile no longer flashes the desktop layout before hydration; `npm run lint` and `npm run build` both passed afterward.
