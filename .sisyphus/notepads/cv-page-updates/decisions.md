# Decisions

- Keep browser print/save-as-PDF instead of implementing true PDF generation.
- Put home, layout, language, download, login, and admin edit actions into one top-left CV control cluster.
- No new automated test infrastructure; verify with lint/build plus browser/manual scripted checks.
- Default layout: minimal below `md`, full at `md` and above; icon-only vertical stack below `sm`.
