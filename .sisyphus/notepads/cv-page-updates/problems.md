# Problems

- `SwitchLanguage` is currently much smaller than adjacent actions and likely needs API/styling refactor to match control sizing.
- `CVPageShell` currently splits controls across top-left and top-right, so responsive/action/auth changes are coupled in one file.
