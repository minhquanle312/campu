Campu is a Next.js application in a git repo. Root package.json shows scripts for dev (`next dev --port 3282`), build (`next build`), start (`next start`), lint (`eslint .`), and format (`prettier --write "src/**/*.ts" "src/**/*.tsx"`). Tech stack visible from package.json includes Next.js 16, React 19, TypeScript, ESLint, Prettier, Tailwind CSS 4, Playwright, and Yarn 4 (`packageManager: yarn@4.13.0`).